# 리팩토링 체크리스트

## 🎯 리팩토링 원칙

> **핵심 규칙**: 리팩토링 중에는 기능을 변경하지 않는다. 모든 테스트가 계속 통과해야 한다.

---

## ✅ 기본 체크리스트

### 1. 테스트 통과 확인

- [ ] 모든 테스트가 통과하는가?
- [ ] 리팩토링 전 테스트 커버리지 확인
- [ ] 리팩토링 후 테스트 커버리지 유지/향상

### 2. 코드 중복 제거 (DRY)

- [ ] 반복되는 코드 블록을 함수로 추출
- [ ] 유사한 로직을 헬퍼 함수로 통합
- [ ] 중복된 상수를 상위로 이동

### 3. 함수/메서드 개선

- [ ] 함수가 단일 책임을 가지는가?
- [ ] 함수 길이가 적절한가? (20줄 이하 권장)
- [ ] 매개변수가 3개 이하인가?
- [ ] 중첩 깊이가 3단계 이하인가?
- [ ] Early return을 사용했는가?

### 4. 네이밍 개선

- [ ] 변수명이 의도를 명확히 표현하는가?
- [ ] 함수명이 동작을 명확히 설명하는가?
- [ ] 약어를 피하고 명확한 이름 사용
- [ ] 이벤트 핸들러는 `handle*` prefix 사용
- [ ] Boolean 변수는 `is*`, `has*`, `should*` prefix 사용

### 5. 타입 안전성 (TypeScript)

- [ ] `any` 타입 사용을 제거했는가?
- [ ] 명시적 타입 정의를 추가했는가?
- [ ] Union type으로 더 정확한 타입 표현
- [ ] Optional chaining (`?.`)과 Nullish coalescing (`??`) 활용

---

## 🏗️ 구조 개선

### 컴포넌트 리팩토링 (React)

- [ ] 컴포넌트가 50줄 이하인가?
- [ ] Props가 명확하게 타입 정의되었는가?
- [ ] 로직과 UI가 적절히 분리되었는가?
- [ ] 커스텀 훅으로 로직을 추출할 수 있는가?
- [ ] 불필요한 re-render를 방지했는가? (useMemo, useCallback)

### Hook 리팩토링

- [ ] Hook이 단일 책임을 가지는가?
- [ ] Hook이 재사용 가능한가?
- [ ] 의존성 배열이 정확한가?
- [ ] 반환 값이 명확한가?

### 유틸리티 함수

- [ ] 순수 함수로 작성되었는가? (부수 효과 없음)
- [ ] 입출력이 명확한가?
- [ ] 적절한 폴더에 위치하는가? (`utils/`)

---

## 🎨 코드 스타일

### 가독성

- [ ] 들여쓰기가 일관적인가?
- [ ] 공백과 줄바꿈이 적절한가?
- [ ] 관련 코드끼리 그룹화되었는가?
- [ ] 복잡한 조건문을 함수로 추출했는가?

### 주석

- [ ] 불필요한 주석을 제거했는가?
- [ ] 남은 주석이 "왜"를 설명하는가? (무엇이 아닌)
- [ ] TODO 주석을 정리했는가?
- [ ] console.log를 제거했는가?

### Import 정리

- [ ] 사용하지 않는 import 제거
- [ ] Import 순서가 ESLint 규칙에 맞는가?
  1. Node.js built-ins
  2. External libraries (알파벳 순)
  3. Internal modules (알파벳 순)
  4. Index files

---

## 🔧 성능 최적화 (필요 시)

- [ ] 불필요한 연산을 memoization 했는가?
- [ ] 큰 리스트를 virtualization 했는가?
- [ ] 이미지 lazy loading을 적용했는가?
- [ ] 무거운 라이브러리를 dynamic import 했는가?

---

## 🧪 테스트 리팩토링

### 테스트 코드 개선

- [ ] 테스트 이름이 명확한가?
- [ ] 중복된 setup 코드를 beforeEach로 추출
- [ ] 테스트 데이터를 fixture로 분리
- [ ] 커스텀 렌더 함수 활용 (renderWithProviders)

### 테스트 가독성

- [ ] AAA 패턴을 따르는가?
- [ ] 각 테스트가 하나의 것만 검증하는가?
- [ ] 매직 넘버/문자열을 상수로 추출

---

## 🚨 린터 및 타입 체크

### ESLint

- [ ] `pnpm lint:eslint` 실행 및 경고 해결
- [ ] 자동 수정 가능한 항목 적용: `pnpm lint:eslint --fix`

### TypeScript

- [ ] `pnpm lint:tsc` 실행 및 타입 에러 해결
- [ ] strict mode 위반 사항 해결

### 전체 린트

- [ ] `pnpm lint` 통과

---

## 🎯 접근성 (A11y)

- [ ] 의미있는 HTML 요소 사용 (시맨틱 태그)
- [ ] 버튼과 링크가 적절히 구분되는가?
- [ ] Form 요소에 label이 있는가?
- [ ] 키보드 네비게이션이 가능한가?
- [ ] Focus 상태가 시각적으로 명확한가?
- [ ] ARIA 속성이 필요한 곳에 적용되었는가?

---

## 📦 의존성 관리

- [ ] 사용하지 않는 패키지 제거
- [ ] 중복된 기능의 라이브러리 통합
- [ ] 패키지 버전 최신화 검토

---

## 📝 문서화

- [ ] 복잡한 로직에 JSDoc 추가
- [ ] README 업데이트 (필요 시)
- [ ] 타입 정의에 주석 추가 (필요 시)

---

## ✔️ 최종 확인

### 리팩토링 완료 전

- [ ] 모든 테스트가 여전히 통과하는가?
- [ ] 린터 경고가 없는가?
- [ ] 타입 에러가 없는가?
- [ ] 기능이 정상 동작하는가?
- [ ] 코드 리뷰 요청 준비 완료

### Git 커밋

- [ ] 의미있는 단위로 커밋 분리
- [ ] 커밋 메시지가 명확한가?
  - `refactor: 중복 코드 제거`
  - `refactor: 컴포넌트 분리 및 hook 추출`
  - `refactor: 타입 안전성 개선`

---

## 🎓 리팩토링 패턴 예시

### 1. 함수 추출 (Extract Function)

```typescript
// Before
function processUser(user) {
  const fullName = user.firstName + ' ' + user.lastName;
  const age = new Date().getFullYear() - user.birthYear;
  return { fullName, age };
}

// After
function getFullName(user) {
  return `${user.firstName} ${user.lastName}`;
}

function calculateAge(birthYear) {
  return new Date().getFullYear() - birthYear;
}

function processUser(user) {
  return {
    fullName: getFullName(user),
    age: calculateAge(user.birthYear),
  };
}
```

### 2. 조건문을 Guard Clause로 (Early Return)

```typescript
// Before
function processOrder(order) {
  if (order.isValid) {
    if (order.isPaid) {
      return shipOrder(order);
    } else {
      return 'Payment required';
    }
  } else {
    return 'Invalid order';
  }
}

// After
function processOrder(order) {
  if (!order.isValid) return 'Invalid order';
  if (!order.isPaid) return 'Payment required';

  return shipOrder(order);
}
```

### 3. 매직 넘버/문자열을 상수로

```typescript
// Before
if (user.age >= 18 && user.age < 65) {
  // ...
}

// After
const ADULT_AGE = 18;
const RETIREMENT_AGE = 65;

if (user.age >= ADULT_AGE && user.age < RETIREMENT_AGE) {
  // ...
}
```

---

## 📚 참고 자료

- Clean Code 원칙: `.cursor/rules/clean-code.mdc`
- React 베스트 프랙티스: `.cursor/rules/react.mdc`
- TypeScript 가이드라인: `.cursor/rules/typescript.mdc`

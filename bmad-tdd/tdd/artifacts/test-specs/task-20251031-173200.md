# 테스트 설계 명세서: 반복 일정 표시

## 📋 기본 정보

- **Task ID**: task-20251031-173200
- **작성일**: 2025-10-31
- **작성자**: Test Designer Agent
- **기능 명세서**: `bmad-tdd/tdd/artifacts/features/task-20251031-173200.md`

---

## 🎯 테스트 전략

### 테스트 범위

- **단위 테스트**:
  - `getRepeatIconLabel` 함수 (툴팁 텍스트 생성)
  - 반복 유형별 레이블 생성 로직
  - interval 처리 로직
- **컴포넌트 테스트**:
  - `RepeatEventIcon` 컴포넌트 렌더링
  - 반복 유형별 아이콘 표시
  - 툴팁 표시 및 상호작용
  - 접근성 속성 검증
- **E2E 테스트**: 불필요 (독립적인 컴포넌트)

### 테스트 우선순위

1. 🔴 **Critical**:

   - 반복 유형별 올바른 아이콘 표시
   - 일반 일정(none)에서 아이콘 미표시
   - 접근성 속성 (aria-label) 존재

2. 🟡 **Important**:

   - 툴팁 텍스트 정확성
   - interval에 따른 동적 레이블
   - hover 시 툴팁 표시

3. 🟢 **Nice to have**:
   - size prop에 따른 크기 변화
   - 테마 색상 적용

---

## 📝 테스트 케이스

### 테스트 케이스 1: 매일 반복 일정 아이콘 표시

**우선순위**: Critical

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'daily', interval: 1 }`

**Act** (실행)

- 컴포넌트가 DOM에 렌더링됨

**Assert** (검증)

- 반복 아이콘이 화면에 표시된다
- `aria-label` 속성이 "반복 일정: 매일"을 포함한다
- `role="img"` 속성이 존재한다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 2: 매주 반복 일정 아이콘 표시

**우선순위**: Critical

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'weekly', interval: 1 }`

**Act** (실행)

- 컴포넌트가 DOM에 렌더링됨

**Assert** (검증)

- 주간 반복 아이콘이 화면에 표시된다
- `aria-label` 속성이 "반복 일정: 매주"를 포함한다
- `role="img"` 속성이 존재한다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 3: 매월 반복 일정 아이콘 표시

**우선순위**: Critical

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'monthly', interval: 1 }`

**Act** (실행)

- 컴포넌트가 DOM에 렌더링됨

**Assert** (검증)

- 월간 반복 아이콘이 화면에 표시된다
- `aria-label` 속성이 "반복 일정: 매월"을 포함한다
- `role="img"` 속성이 존재한다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 4: 매년 반복 일정 아이콘 표시

**우선순위**: Critical

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'yearly', interval: 1 }`

**Act** (실행)

- 컴포넌트가 DOM에 렌더링됨

**Assert** (검증)

- 연간 반복 아이콘이 화면에 표시된다
- `aria-label` 속성이 "반복 일정: 매년"을 포함한다
- `role="img"` 속성이 존재한다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 5: 일반 일정(none)에서 null 반환

**우선순위**: Critical

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'none' }`

**Act** (실행)

- 컴포넌트가 렌더링을 시도함

**Assert** (검증)

- 아무것도 렌더링되지 않는다 (null 반환)
- 화면에 아이콘이 표시되지 않는다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 6: hover 시 툴팁 표시 - 매주

**우선순위**: Important

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'weekly', interval: 1 }`
- userEvent 설정

**Act** (실행)

- 사용자가 아이콘에 마우스를 올린다 (hover)

**Assert** (검증)

- 툴팁이 표시된다
- 툴팁 텍스트가 "매주 반복"이다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 7: interval이 1보다 큰 경우 툴팁 - 2주마다

**우선순위**: Important

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'weekly', interval: 2 }`
- userEvent 설정

**Act** (실행)

- 사용자가 아이콘에 마우스를 올린다 (hover)

**Assert** (검증)

- 툴팁이 표시된다
- 툴팁 텍스트가 "2주마다 반복"이다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 8: interval이 1보다 큰 경우 툴팁 - 3일마다

**우선순위**: Important

**Arrange** (준비)

- `RepeatEventIcon` 컴포넌트를 렌더링
- Props: `{ repeatType: 'daily', interval: 3 }`
- userEvent 설정

**Act** (실행)

- 사용자가 아이콘에 마우스를 올린다 (hover)

**Assert** (검증)

- 툴팁이 표시된다
- 툴팁 텍스트가 "3일마다 반복"이다

**테스트 파일**: `src/__tests__/components/RepeatEventIcon.spec.tsx`

---

### 테스트 케이스 9: getRepeatIconLabel - 매일 반복

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'daily'`, `interval: 1`

**Act** (실행)

- `getRepeatIconLabel('daily', 1)` 호출

**Assert** (검증)

- 반환값이 "매일 반복"이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

### 테스트 케이스 10: getRepeatIconLabel - 매주 반복

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'weekly'`, `interval: 1`

**Act** (실행)

- `getRepeatIconLabel('weekly', 1)` 호출

**Assert** (검증)

- 반환값이 "매주 반복"이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

### 테스트 케이스 11: getRepeatIconLabel - 매월 반복

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'monthly'`, `interval: 1`

**Act** (실행)

- `getRepeatIconLabel('monthly', 1)` 호출

**Assert** (검증)

- 반환값이 "매월 반복"이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

### 테스트 케이스 12: getRepeatIconLabel - 매년 반복

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'yearly'`, `interval: 1`

**Act** (실행)

- `getRepeatIconLabel('yearly', 1)` 호출

**Assert** (검증)

- 반환값이 "매년 반복"이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

### 테스트 케이스 13: getRepeatIconLabel - 2주마다 반복

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'weekly'`, `interval: 2`

**Act** (실행)

- `getRepeatIconLabel('weekly', 2)` 호출

**Assert** (검증)

- 반환값이 "2주마다 반복"이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

### 테스트 케이스 14: getRepeatIconLabel - 3일마다 반복

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'daily'`, `interval: 3`

**Act** (실행)

- `getRepeatIconLabel('daily', 3)` 호출

**Assert** (검증)

- 반환값이 "3일마다 반복"이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

### 테스트 케이스 15: getRepeatIconLabel - none일 경우

**우선순위**: Important

**Arrange** (준비)

- 함수 호출 준비
- 인자: `repeatType: 'none'`, `interval: 1`

**Act** (실행)

- `getRepeatIconLabel('none', 1)` 호출

**Assert** (검증)

- 반환값이 빈 문자열("")이다

**테스트 파일**: `src/__tests__/unit/easy.repeatEventDisplay.spec.ts`

---

## 🧪 테스트 데이터 정의

### Mock 데이터

```typescript
// 테스트에 사용할 반복 유형
const repeatTypes = {
  none: 'none' as RepeatType,
  daily: 'daily' as RepeatType,
  weekly: 'weekly' as RepeatType,
  monthly: 'monthly' as RepeatType,
  yearly: 'yearly' as RepeatType,
};

// 테스트용 Props
const defaultProps = {
  repeatType: 'weekly' as RepeatType,
  interval: 1,
  size: 'small' as const,
};

const customIntervalProps = {
  repeatType: 'weekly' as RepeatType,
  interval: 2,
  size: 'small' as const,
};
```

### Fixture 데이터

```typescript
// 반복 유형별 예상 레이블
const expectedLabels = {
  daily: '매일 반복',
  weekly: '매주 반복',
  monthly: '매월 반복',
  yearly: '매년 반복',
  none: '',
};

// interval별 예상 레이블
const customIntervalLabels = {
  daily2: '2일마다 반복',
  daily3: '3일마다 반복',
  weekly2: '2주마다 반복',
  weekly3: '3주마다 반복',
  monthly2: '2개월마다 반복',
  yearly2: '2년마다 반복',
};
```

---

## 🎭 Mock/Stub 전략

### 컴포넌트 Mocking

이 테스트는 독립적인 컴포넌트이므로 특별한 mocking이 필요하지 않습니다.
단, Material-UI의 Tooltip 컴포넌트는 실제로 사용하여 상호작용을 테스트합니다.

---

## 📁 테스트 파일 구조

```
src/__tests__/
  ├── components/
  │   └── RepeatEventIcon.spec.tsx      # 컴포넌트 테스트
  └── unit/
      └── easy.repeatEventDisplay.spec.ts  # 유틸 함수 테스트
```

### 예상 테스트 파일 목록

1. `src/__tests__/components/RepeatEventIcon.spec.tsx` - RepeatEventIcon 컴포넌트 테스트
   - 반복 유형별 아이콘 렌더링 (5개)
   - 툴팁 표시 및 상호작용 (3개)
   - 접근성 속성 검증 (포함)
2. `src/__tests__/unit/easy.repeatEventDisplay.spec.ts` - getRepeatIconLabel 함수 테스트
   - 반복 유형별 레이블 생성 (5개)
   - interval 처리 (2개)

---

## 🎯 React Testing Library 쿼리 전략

### 사용할 쿼리 (우선순위 순)

1. **getByRole**: `screen.getByRole('img', { name: /반복 일정/ })`

   - 아이콘의 role="img" 속성을 통해 접근
   - aria-label을 name 옵션으로 검증

2. **getByLabelText**: 필요 시 사용

   - aria-label 기반 검색

3. **getByText**: `screen.getByText(/매주 반복/)`

   - 툴팁 텍스트 검증

4. **getByTestId**: 사용하지 않음 (접근성 우선)

### 비동기 처리

- **findByText**: 툴팁이 비동기로 나타날 때 사용

  ```typescript
  const tooltip = await screen.findByText('매주 반복');
  ```

- **userEvent**: 사용자 상호작용 시뮬레이션
  ```typescript
  const user = userEvent.setup();
  await user.hover(icon);
  ```

---

## ⚠️ 엣지 케이스 테스트

### 1. interval이 1이 아닌 경우 (이미 테스트 케이스 7, 8에 포함)

- **시나리오**: interval이 2 이상인 경우
- **검증 항목**:
  - 툴팁 텍스트가 "{interval}{단위}마다 반복" 형식인가?
  - daily: "3일마다 반복"
  - weekly: "2주마다 반복"
  - monthly: "2개월마다 반복"
  - yearly: "2년마다 반복"

### 2. repeatType이 'none'인 경우 (이미 테스트 케이스 5에 포함)

- **시나리오**: 일반 일정 (반복 없음)
- **검증 항목**:
  - 컴포넌트가 null을 반환하는가?
  - 아이콘이 DOM에 렌더링되지 않는가?

### 3. size prop이 다른 경우

- **시나리오**: size='small', 'medium', 'large' 전달
- **검증 항목**:
  - 아이콘이 정상적으로 렌더링되는가?
  - (스타일링은 통합 테스트에서 검증, 단위 테스트는 렌더링만)

### 4. interval이 0 또는 음수인 경우

- **시나리오**: 비정상적인 interval 값 (방어 코드)
- **검증 항목**:
  - 기본값 1로 처리하거나 에러 처리
  - "매주 반복"으로 표시 (interval=1로 간주)

### 5. repeatType이 유효하지 않은 값인 경우

- **시나리오**: TypeScript 타입 시스템으로 방지됨
- **검증 항목**:
  - 런타임에서는 'none'처럼 처리 (null 반환)
  - 또는 기본 아이콘 표시

---

## 🚨 에러 처리 테스트

이 기능은 표시 전용 컴포넌트이므로 특별한 에러 처리가 필요하지 않습니다.

### 방어 코드 테스트

1. **interval이 undefined인 경우**

   - **발생 조건**: interval prop이 전달되지 않음
   - **예상 동작**: 기본값 1 사용
   - **검증 방법**: 기본 Props로 테스트 (케이스 1-4)

2. **repeatType이 잘못된 경우** (TypeScript로 방지)
   - **발생 조건**: 타입 시스템 우회
   - **예상 동작**: null 반환 또는 기본 처리
   - **검증 방법**: 추가 케이스 작성 (선택사항)

---

## 📋 테스트 체크리스트

### 테스트 작성 전

- [x] 기능 명세서 검토 완료
- [x] 테스트 케이스가 인수 조건을 모두 커버하는가? (12개 조건 → 15개 테스트)
- [x] Mock 전략이 명확한가? (mocking 불필요)
- [x] 테스트 데이터가 준비되었는가? (Mock/Fixture 데이터 정의됨)

### 테스트 작성 중

- [ ] AAA 패턴을 따르는가?
- [ ] 접근성 쿼리를 우선 사용하는가? (getByRole 사용)
- [ ] userEvent를 사용하는가? (hover 테스트)
- [ ] 비동기 처리가 적절한가? (findByText 사용)

### 테스트 작성 후

- [ ] 모든 테스트가 실패하는가? (RED 단계)
- [ ] 테스트 이름이 명확한가?
- [ ] 중복 코드가 없는가?
- [ ] 테스트가 독립적으로 실행 가능한가?

---

## 📊 테스트 커버리지 목표

### 인수 조건 커버리지

| 인수 조건                               | 테스트 케이스   | 상태 |
| --------------------------------------- | --------------- | ---- |
| 1. repeat.type !== 'none'에 아이콘 표시 | TC 1-4          | ✓    |
| 2. 반복 유형별 다른 아이콘              | TC 1-4          | ✓    |
| 3. none에 아이콘 미표시                 | TC 5            | ✓    |
| 4. 아이콘은 제목 앞/옆 표시             | (통합 테스트)   | -    |
| 5. 아이콘 크기 통일                     | (시각적 테스트) | -    |
| 6. 아이콘 색상 조화                     | (시각적 테스트) | -    |
| 7. 반응형 디자인                        | (통합 테스트)   | -    |
| 8. hover 시 툴팁 표시                   | TC 6-8          | ✓    |
| 9. aria-label 포함                      | TC 1-4          | ✓    |
| 10. 텍스트 대체 수단                    | TC 6-8 (툴팁)   | ✓    |
| 11. 성능 저하 없음                      | (성능 테스트)   | -    |
| 12. 많은 일정에도 부드럽게              | (성능 테스트)   | -    |

**커버리지**: 8/12 (67%) - 단위/컴포넌트 테스트 범위
나머지는 통합/E2E 테스트 또는 시각적 테스트에서 검증

---

## 📚 참고 문서

- TDD 워크플로우 가이드: `docs/tdd-workflow-guide.md`
- 기능 명세서: `bmad-tdd/tdd/artifacts/features/task-20251031-173200.md`
- React Testing Library 문서: https://testing-library.com/docs/react-testing-library/intro/
- Material-UI Icons: https://mui.com/material-ui/material-icons/
